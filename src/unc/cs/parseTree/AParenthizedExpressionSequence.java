package unc.cs.parseTree;

import java.util.List;
// for now, we will not use it, and simply rely on regexes
public class AParenthizedExpressionSequence extends ANodesCollection{

	public AParenthizedExpressionSequence(List<CheckedNode> aNodes) {
		super(aNodes);
	}

}
